'''
    if held_keys['left shift']:
		player.speed = 10

	if not held_keys['left shift']:
		player.speed = 5

	if held_keys['r']:
		player.gravity = -1

	if not held_keys['r']:
		player.gravity = 1

	if held_keys['t']:
		player.gravity = 0

	if not held_keys['t']:
		player.gravity = 1
'''
